from fastapi import FastAPI, WebSocket\nfrom fastapi.responses import HTMLResponse\nfrom fastapi.middleware.cors import CORSMiddleware\nfrom typing import List\n\napp = FastAPI()\n\n# Allow CORS (Cross-Origin Resource Sharing) for all origins\napp.add_middleware(\n    CORSMiddleware,\n    allow_origins=["*"],\n    allow_credentials=True,\n    allow_methods=["*"],\n    allow_headers=["*"],\n)\n\n# Example REST API endpoint\n@app.get("/api/portfolio")\nasync def get_portfolio():\n    return {"portfolio": "This is a sample portfolio data."}\n\n# WebSocket for live portfolio updates\n@app.websocket("/ws/portfolio")\nasync def websocket_portfolio_updates(websocket: WebSocket):\n    await websocket.accept()\n    while True:\n        # Here you would implement logic to send real-time updates\n        await websocket.send_text("Live portfolio update!")\n